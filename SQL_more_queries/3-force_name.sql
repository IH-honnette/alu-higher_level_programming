-- Create the table force_name if it does not already exist
CREATE TABLE IF NOT EXISTS force_name (
    id INT,                   -- Column id of type INT
    name VARCHAR(256) NOT NULL -- Column name of type VARCHAR(256) that cannot be NULL
);

